Icon icon = Icon.ExtractAssociatedIcon(txtFilePath.Text);
아이콘 이걸로 추출함

파일안의 정보 기록 순서





id - bm

사용자 표시 이름
경로
타입

//(파일 이름이다)

(첫번째줄)파일타입 0 1 2, 프로그램, 폴더, 사이트

(두번째줄)파일경로 ~~~~~~

예
아이디3의 텍스트파일
C:\UAM\opt_prog\dat_id\id3.txt
0

id - pc
이름
파일 이름
경로 끝
2줄은 줄까?


파일 이름 ? 필요? 위에 같이 써버리면 된다
폴더랑 사이트는 파일 경로 필요 X - 2번째 줄에 그냥 주소를 쓰면 된다, 폴더 주소나, 사이트 주소
파일 이름을 별도 표기?, 사실 북마크 모드에서는 필요 없다


내일 투두

파일 등록할 때 
id 파일과 릴레이션 폴더를 만든다


할것 오전
파일 실행할 때, 연관관계 폴더에 있는지 체크하고 없으면 만들어서 증가(나우에)
파일 실행 당할 때, 연관관계 파일 만들고 증가시켜줌(넥스트에)



추천리스트뷰 - 메인뷰처럼 쭉 읽어와서 sorting 하고 출력해주도록 만들면 될듯?

프로세스 이벤트 감지하는거 만들것


            //FileStream fs = new FileStream(searchPath, FileMode.OpenOrCreate, FileAccess.ReadWrite);//없으면 만들고 읽고쓰기

            //FileStream fsr = new FileStream(searchPath, FileMode.OpenOrCreate, FileAccess.Read);
            //StreamReader sr = new StreamReader(fsr, Encoding.Default, true);
            StreamReader sr = new StreamReader(searchPath, Encoding.Default, true);

            if (!String.IsNullOrEmpty((tmpLine = sr.ReadLine())))
            {
                //sr.Dispose();
                sr.Close();
                //fsr.Close();
                //증가처리
                Int32.TryParse(tmpLine, out tmpDat);
                tmpDat++;
                tmpLine = Convert.ToString(tmpDat);
                //쓰기
                //FileStream fsw = new FileStream(searchPath, FileMode.Truncate, FileAccess.Write);
                //StreamWriter sw = new StreamWriter(fsw, Encoding.Default);
                StreamWriter sw = new StreamWriter(searchPath, false, Encoding.Default);
                sw.WriteLine(tmpLine);
                sw.Close();
            }
            else
            {
                sr.Close();
                //fsr.Close();
                //새 값 1 쓰기
                //FileStream fsw = new FileStream(searchPath, FileMode.Truncate, FileAccess.Write);
                //StreamWriter sw = new StreamWriter(fsw, Encoding.Default);
                StreamWriter sw = new StreamWriter(searchPath, false);
                sw.WriteLine("1");
                sw.Close();
            }

			c#은 직접 참조에서 라이브러리 추가해줘야 할 때가 있다.


			dllhost.exe-거름

			파일 이름은 받을땐 그냥 받고 받은곳에서 다듬을것? ㄴㄴ 그냥 다듬어서 준다

			
			


			초기기동 기초설정 할것
			연관통계출력
			타임테이블출력
			삭제기능 테스트
			통계 추가 기능/\?